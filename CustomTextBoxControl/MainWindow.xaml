<Window x:Class="CustomTextBoxControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CustomTextBoxControl"
        xmlns:userControls="clr-namespace:CustomTextBoxControl.View.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="400" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>

        <TextBlock Text="Form" FontSize="20" VerticalAlignment="Center" 
                   HorizontalAlignment="Center"/>
        
        <userControls:ClearableTextBox Grid.Row="1" Width="250" Height="40" Placeholder="First name"/>
        <userControls:ClearableTextBox Grid.Row="2" Width="250" Height="40" Placeholder="Last name"/>
        <userControls:ClearableTextBox Grid.Row="3" Width="250" Height="40" Placeholder="Age"/>
        <userControls:ClearableTextBox Grid.Row="4" Width="250" Height="40" Placeholder="Email"/>

        <TextBlock Grid.Row="6" Text="Footer" FontSize="20" 
                   VerticalAlignment="Center" HorizontalAlignment="Center"/>
    </Grid>
</Window>
